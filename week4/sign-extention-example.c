
#include <stdio.h>

int main() {

	short int x =  15213;
  int      ix = (int) x; 
  short int y = -15213;
  int      iy = (int) y;

	printf("x: %d, ix: %d, y: %d, iy: %d", x, ix, y, iy);

	return 0;
}
